---
file_description: "General header which should be included to use the TSL."
includes: ['"static/tsl_static.h"', '"generated/tsl_generated.h"']
implementations:
  - |
    #ifndef TSL_GIT_VERSION_STR
    #   define TSL_GIT_VERSION_STR "{{ git_version_str }}"
    #endif
...
