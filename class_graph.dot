strict digraph  {
calc;
convert;
ls;
mask_ls;
compare;
misc;
mask;
memory;
io;
binary;
convert -> calc;
ls -> calc;
ls -> misc;
ls -> mask_ls;
ls -> mask;
ls -> binary;
mask_ls -> calc;
mask_ls -> compare;
mask -> misc;
mask -> mask_ls;
}
