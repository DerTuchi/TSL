strict digraph  {
ls;
calc;
convert;
io;
memory;
binary;
mask_ls;
mask;
compare;
misc;
ls -> calc;
ls -> binary;
ls -> mask_ls;
ls -> mask;
ls -> misc;
convert -> calc;
mask_ls -> compare;
mask -> mask_ls;
mask -> misc;
}
