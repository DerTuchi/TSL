strict digraph  {
memory;
mask;
ls;
misc;
calc;
convert;
binary;
io;
mask_ls;
compare;
mask -> misc;
mask -> mask_ls;
ls -> mask;
ls -> misc;
ls -> calc;
ls -> binary;
ls -> mask_ls;
convert -> calc;
mask_ls -> compare;
}
